# 应用服务 WEB 访问端口
server:
  port: 8090
spring:
  # redis配置
  data:
    redis:
#      host: 8.129.7.16
      host: 127.0.0.1
      port: 6379
      timeout: 5s
      database: 0
      # redis1121
      password: dajusheng@20241201.
#      password:
    # 连接超时时间
    jedis:
      pool:
        # 连接池中的最小空闲连接
        min-idle: 1
        # 连接池中的最大空闲连接
        max-idle: 50
        # 连接池的最大数据库连接数
        max-active: 200
        # #连接池最大阻塞等待时间（使用负值表示没有限制）
        max-wait: -1

# 为不同业务设置不同的数据库
redis:
  database:
    businessUser: 1